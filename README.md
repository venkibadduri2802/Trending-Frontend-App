# 🚀 GitHub Trending Repositories - Frontend (React + Next.js)

This React/Next.js app lists the **most starred GitHub repositories created in the last 10 days**.  
It communicates with a .NET backend API that proxies GitHub search requests and supports pagination.

---

## 🧰 Tech Stack

- **Next.js 16**
- **React 18**
- **TypeScript**
- **Tailwind CSS**
- **Axios**

---

## ⚙️ Setup Instructions

```bash
# Go inside frontend folder
cd trendingappfrontend

# Install dependencies
npm install

# Run locally
npm run dev
Visit → http://localhost:3000

⚙️ Environment Configuration

Create a .env.local file:

NEXT_PUBLIC_API_URL=https://localhost:7010


This value points to your backend API base URL.

📁 Key Files Overview
File	Description
pages/index.tsx	Entry page that renders the RepoList component
components/RepoList.tsx	Fetches paginated GitHub data and handles “Load More” logic
components/RepoItem.tsx	Displays individual repository details
lib/api.ts	Axios instance and API call to backend
styles/globals.css	Tailwind global styles and layout
```

---

🧮 Pagination Logic

Displays 30 repositories per page

Uses GitHub Search API (via backend proxy)

Loads next 30 results when clicking “Load More”

Date dynamically computed as today minus 10 days

Example API call generated by frontend:

https://localhost:7010/api/Github/search?date=2025-11-01&page=1&per_page=30

---

🎨 UI & Styling

    1. Built using Tailwind CSS

    2. Responsive layout for all screen sizes

    3. Header/Footer with neutral tone (#efeeec)

    4. Clean borders, padding, and spacing for each repository card

---

🧪 Testing Steps

1. Start backend (https://localhost:7010)

2. Start frontend (http://localhost:3000)

3. Verify:

   30 repositories load on first page

   Clicking Load More fetches next 30

   Correct date range (current date - 10 days)

**\*\*** Author ****\*\*****

Kiranmai
Senior .NET / React Developer
Kuala Lumpur, Malaysia
